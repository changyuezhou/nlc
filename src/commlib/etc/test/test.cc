// Copyright (c) 2013 zhou chang yue. All rights reserved.

#include <string>
#include "commlib/etc/inc/conf.h"
#include "commlib/etc/inc/log.h"
//#include "external/gtest/include/gtest/gtest.h"

using lib::etc::etc;
using lib::etc::ini;
using lib::etc::conf;
/*
TEST(conf, LoadFile) {
  conf config;
  EXPECT_EQ(0, config.Create("test.conf"));
  config.Dump();
  // EXPECT_EQ(-1, config.Create("test.ini"));
}
 */
